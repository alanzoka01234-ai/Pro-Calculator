
export type Operation = '+' | '-' | '*' | '/' | null;

export enum ButtonType {
  NUMBER = 'number',
  OPERATOR = 'operator',
  ACTION = 'action',
  EQUALS = 'equals'
}

export type Theme = 'light' | 'dark';
